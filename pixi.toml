[workspace]
authors = ["吴一凡 <1007372080@qq.com>"]
channels = ["conda-forge"]
name = "因子研究"
platforms = ["win-64", "linux-64"]
version = "0.1.0"

[tasks]
lint       = "ruff check ."
format     = "ruff format ."
test       = "pytest tests/ -v"
typecheck  = "mypy common daily/evaluation daily/preprocessing"
lab        = "jupyter lab"
smoke      = "python -c \"import polars; import tushare; print('ok')\""
report     = "python scripts/generate_report.py"

# runtime 依赖
[dependencies]
python = ">=3.10,<3.13"

[pypi-dependencies]
polars       = ">=1.0.0"
pyarrow      = ">=14.0.0"
tushare      = ">=1.4.0"
pandas       = ">=2.0.0"
numpy        = ">=1.24.0"
scipy        = ">=1.11.0"
cvxpy        = ">=1.4"
optuna       = ">=3.5"
statsmodels  = ">=0.14.0"
matplotlib   = ">=3.7.0"
jinja2       = ">=3.1.0"
# 本地包 editable install（消除 scripts/ 中的 sys.path hack）
factor-research = { path = ".", editable = true }
apscheduler      = ">=3.10"
pydantic         = ">=2.5"

# dev 依赖（测试 / lint / notebook）
[feature.dev.pypi-dependencies]
pytest     = ">=7.0.0"
ruff       = ">=0.3.0"
mypy       = ">=1.9.0"
jupyterlab = ">=4.0.0"

[environments]
# 默认环境包含 dev 特性，保持开发体验不变
default = { features = ["dev"], solve-group = "default" }
