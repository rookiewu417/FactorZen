"""日终任务自动化调度原型（早期 DAG 编排，非当前生产路径）。

本子包（dag.py / jobs.py / state.py）是早于现行 M0-M7 路线图的任务编排原型：
一次目录重构（c98e1c3）删除了配套的 CLI 入口脚本，模块本体被一并搬进了现在的
`src/factorzen/` 结构，但没有任何 `fz` 命令能到达这里。代码与测试予以保留，
供未来可能需要日终调度时参考，目前不是生产路径的一部分。
"""
